import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.eventicket.app',
  appName: 'EvenTicket',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
